package pingping.Database.OrmObjects;

public class Subscription {
    // TODO create new subscription base class that TwitchSub and YoutubeSub will extend from
    // TODO make SubscriptionsEmbed accept base class Subscription (because it does not need the eventsub id)
}
