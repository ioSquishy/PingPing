package pingping.Youtube;

public class LivePoller {
    // TODO create a thread that polls channels in database every minute
    // TODO create database table columns: server_id, broadcaster_id, pingrole_id, pingchannel_id, uploads_playlist_id, user_handle, last_live_vid_id
    // to prevent duplicate notifications, store/check last_live_vid_id
}
